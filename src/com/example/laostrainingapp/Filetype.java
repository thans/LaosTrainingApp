package com.example.laostrainingapp;

/**
 * Represents file types
 * Currently files are only treated as images, videos, text, csv, or unsupported
 * @author toreh
 */
public enum Filetype {
	IMAGE, VIDEO, TEXT, CSV, UNSUPPORTED;
}
